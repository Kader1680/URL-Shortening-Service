# generate random integer values
from random import seed
rom random import randint
#f (1)
# generate some integers
for _ in range(10):
	value = randint(0, 10)
	print(value)seed random number generator
seed